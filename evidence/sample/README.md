# Sample evidence only

These files illustrate the expected evidence shape and are **not claimed to be a genuine LLM discovery run**. Run the commands in the repository README with `OPENAI_API_KEY` to generate genuine evidence under `evidence/live/` before submission.
